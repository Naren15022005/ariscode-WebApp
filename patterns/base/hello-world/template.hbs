// {{projectName}} — generated by Aris Code
// Framework: {{framework}} | Language: {{language}}

{{#if (eq language "typescript")}}
const greet = (name: string): string => {
  return `Hello, ${name}! Welcome to {{projectName}}.`;
};

console.log(greet('World'));
{{else if (eq language "javascript")}}
const greet = (name) => {
  return `Hello, ${name}! Welcome to {{projectName}}.`;
};

console.log(greet('World'));
{{else if (eq language "python")}}
def greet(name: str) -> str:
    return f"Hello, {name}! Welcome to {{projectName}}."

print(greet("World"))
{{else}}
echo "Hello, World! Welcome to {{projectName}}.";
{{/if}}
