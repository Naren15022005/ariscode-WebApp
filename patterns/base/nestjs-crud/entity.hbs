import { Entity, Column, PrimaryGeneratedColumn, CreateDateColumn, UpdateDateColumn } from 'typeorm';

@Entity('{{snakeCase entityName}}s')
export class {{pascalCase entityName}} {
  @PrimaryGeneratedColumn('uuid')
  id: string;

{{#each fields}}
  @Column({{#if this.nullable}}{ nullable: true }{{/if}})
  {{camelCase this.name}}: {{this.type}};

{{/each}}
  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;
}
